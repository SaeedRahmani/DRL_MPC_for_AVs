# RLlib-optimized MPC_RL_for_AVs

## TODO

- [x] Collision avoidance
  - [ ] Manual one
  - [ ] In the cost (v1 should always be in the cost)
  - [ ] In the constraint
  - [ ] Enable/disable in the training. 

## Install locally

1. pip install -r requirements.txt
2. go to folder highway-env --> pip install -e .
